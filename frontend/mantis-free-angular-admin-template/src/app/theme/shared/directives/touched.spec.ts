import { Touched } from './touched.directive';

describe('Touched', () => {
  it('should create an instance', () => {
    const directive = new Touched();
    expect(directive).toBeTruthy();
  });
});
